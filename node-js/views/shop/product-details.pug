extends ../templates/main.pug

block content
  main.details
    h1 #{product.title}
    hr
    div
      img(src=product.imageUrl)
    h2 #{product.price}$
    p #{product.description}
    include ../templates/add-to-cartForm.pug

  

